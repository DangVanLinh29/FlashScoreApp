<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="56dp"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/image_player_placeholder"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:src="@drawable/ic_person"
        app:tint="?android:attr/textColorPrimary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/text_player_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="10sp"
        android:textColor="?android:attr/textColorPrimary"
        app:layout_constraintTop_toBottomOf="@id/image_player_placeholder"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Cầu thủ" />

    <TextView
        android:id="@+id/text_player_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="10sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="@id/image_player_placeholder"
        app:layout_constraintBottom_toBottomOf="@id/image_player_placeholder"
        app:layout_constraintStart_toStartOf="@id/image_player_placeholder"
        app:layout_constraintEnd_toEndOf="@id/image_player_placeholder"
        tools:text="99" />

</androidx.constraintlayout.widget.ConstraintLayout>